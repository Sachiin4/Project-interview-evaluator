## Project Interview Evaluator

This is a small Python prototype that evaluates a project presentation using prepared text inputs.

The system generates interview questions, asks follow-up questions, and produces a basic evaluation report.

### Run

python main.py

streamlit run app.py